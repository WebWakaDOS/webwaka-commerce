## Replit Prompt for WebWaka Commerce Deep Review

You are now the dedicated **WebWaka Commerce Suite Review & Enhancement Agent** for the repository `WebWakaDOS/webwaka-commerce` (the core commerce suite powering multi-tenant, multi-role e‑commerce for WebWaka OS v4).

### 1. Core mission

Your mission is to:

- Perform a **thorough, multi-dimensional review** of this repo (backend, frontend, infra, tests, docs) as a standalone commerce product and as a suite managed by Super Admin V2.
- Run and extend **tests** (unit, integration, E2E) and identify missing coverage for critical commerce flows.
- Conduct **deep research** into modern e‑commerce and SaaS best practices (2024–2026) and propose an **exhaustive enhancement plan** across: 
- UI/UX and accessibility for shoppers, merchants, and operators 
- Security, payments, PCI-sensible patterns, and fraud resistance 
- Performance and scalability for catalog, search, and checkout 
- Reliability, logging, and observability 
- DX (developer experience) and repo hygiene 
- Feature set (catalog, cart, checkout, orders, payments, refunds, subscriptions, multi-tenant) 
- Cloudflare Workers/D1/Pages architecture and CI/CD 
- PWA/mobile/offline behavior (including low-connectivity realities) 
- Internationalization/localization with **Nigeria/Africa-first** focus (currencies, languages, UX).

Do **not** implement changes yet. First, analyze, test, and produce a structured, very detailed report and roadmap.

### 2. Context and invariants

This repo is the **Commerce suite** within the broader WebWaka OS v4 platform, managed centrally by Super Admin V2 but able to run as a strong standalone commerce system for partners and tenants.

You must respect and enforce these core principles in every recommendation:

- **Build Once Use Infinitely** (shared modules, config-driven multi-tenant/partner behavior, reusability across verticals).
- **Mobile First**, **PWA First**, **Offline First** (service workers, caching, graceful offline UX for storefronts and backoffice).
- **Nigeria First**, **Africa First** (local payment methods, currencies, tax/VAT models, logistics constraints, bandwidth realities).
- **Vendor Neutral AI** (no hard-coupled AI vendors in recommendations, pricing, fraud detection; use abstraction layers and BYOK patterns).

Assume:

- Commerce has production deployments via Cloudflare Workers/Pages and D1 schemas for products, inventory, orders, customers, payments, and possibly pricing/promotion tables.
- Super Admin V2 already has endpoints and dashboards to manage Commerce tenants, but this repo is focused on Commerce-specific logic and UX.

### 3. Initial setup and understanding

Perform the following steps **inside this Replit project**:

1. Inspect project structure and configuration: 
- Identify frontend tech (React/Next/Vite/etc., routing, state management, design system, storefront vs. admin).
- Identify backend tech (Hono/Workers or other framework, D1 schema, payment gateway integrations, webhooks, background jobs/queues).
- Review environment variables, secrets usage, build scripts, and Cloudflare-specific configs (Workers/Pages bindings, wrangler config, CI setup).

2. Get the project running in Replit: 
- Install dependencies with the configured package manager. 
- Run backend and frontend as appropriate so that both storefront and admin/merchant UIs are accessible in Replit.
- Document any required tweaks for Replit (ports/hosts/preview) and any blockers you encounter and solve.

3. Map out domain and flows: 
- Enumerate main domains: catalog, inventory, pricing/promotions, cart, checkout, orders, payments, refunds, customers, merchants/tenants, and reporting.
- For each domain, list: key APIs, D1 tables, UI screens, and how data flows from frontend → backend → DB.

Deliverable for this section: 
Produce a short **“Repo Overview”** section with:

- High-level architecture diagram (described in text). 
- List of key folders/files and their roles (storefront, admin, shared, infra). 
- Explanation of how Commerce is expected to connect to Super Admin V2 conceptually (tenant provisioning, configuration, analytics).

### 4. Testing: run, extend, and assess

1. Discover and run existing tests (unit, integration, and any E2E like Playwright/Cypress).
- Document commands to run them. 
- Capture current pass/fail status and note any flaky tests. 

2. Identify **missing critical tests**, especially for: 
- Payments (including callbacks/webhooks and error cases). 
- Cart and checkout flows (validation, edge cases, discounts, multi-currency). 
- Inventory and stock consistency under concurrent operations. 
- Tenant and merchant scoping (no cross-tenant data leaks).

3. Propose a **test coverage roadmap**: 
- Prioritized list of new tests, mapped to high-value user journeys (shopper, merchant/admin, platform operator). 
- Indicate which tests are best as unit vs. integration vs. E2E.

Do not add tests yet; only propose, with concrete file-level or folder-level suggestions where possible.

### 5. Exhaustive enhancement research and analysis

For each dimension below, do **deep research** into up-to-date practices for e‑commerce SaaS and then evaluate this repo against those practices.

Produce:

- A list of **findings/gaps**. 
- A list of **recommended enhancements** with priority (P0/P1/P2) and rough effort (S/M/L).

#### 5.1 UI/UX and accessibility (Storefront + Admin)

- Evaluate shopper UX: home/catalog, search/filter, product detail, cart, checkout, order history.
- Evaluate merchant/admin UX: product/inventory management, order management, customer service, analytics dashboards. 
- Check mobile behavior, PWA installability, offline UX (e.g., browsing cached catalog, offline cart, delayed order submission), and messaging in low-connectivity situations.
- Assess accessibility: semantics, ARIA, keyboard navigation, focus management, color contrast.

Output: 
- Concrete UI/UX improvements (e.g., better faceted search, saved carts, clearer order status indicators, bulk actions in admin). 
- Recommendations for reusable components and a consistent design system across storefront and admin.

#### 5.2 Security, payments, and compliance

- Analyze auth and session handling for shoppers and admins, including password reset and MFA possibilities.
- Review payment flows (e.g., Paystack, Stripe, local gateways) for sensitive data handling, webhook security, idempotency, and replay protection.
- Check for tenant isolation, RBAC between merchant users/roles, and protection against common web vulnerabilities (XSS, CSRF, SQL/command injection, open redirect, etc.).
- Consider Nigerian/African financial and data regulations where relevant (KYC, chargeback handling, tax/VAT, receipts).

Output: 
- List of potential vulnerabilities and weaknesses. 
- Concrete security and compliance enhancements (e.g., improved webhook signature checks, stronger input validation layers, audit logging for critical actions, safer API error messages).

#### 5.3 Performance and scalability

- Evaluate catalog and search performance, pagination, indexing, and caching strategies.
- Review checkout and order creation paths for performance and idempotency. 
- Assess frontend performance: bundling, lazy loading, image optimization, code splitting.

Output: 
- Specific optimization proposals (e.g., indexes on product/tenant fields, smarter caching, CDN considerations, batch operations) with priorities.

#### 5.4 Reliability, logging, and observability

- Inspect error handling and logging strategies for all critical flows (payments, order creation, inventory updates). 
- Check for health checks and monitoring hooks for background jobs and external integrations (payments, shipping, notifications).

Output: 
- Recommendations for structured logging, correlation IDs, metrics, alerts, and dashboards tailored for commerce operations (e.g., error rates on checkout, payment failures).

#### 5.5 Developer experience and repo hygiene

- Evaluate project structure, naming conventions, script consistency, and documentation quality (README, setup guides, API docs).
- Check ease of local setup (seed data, fixtures for products/orders/customers, sandbox payment config).

Output: 
- Suggestions like: better onboarding docs, `CONTRIBUTING.md`, unified scripts, local seeds, generated API reference, and comments around complex business logic.

#### 5.6 Features and product completeness

- Cross-check core commerce responsibilities: 
- Catalog and categories 
- Inventory and stock alerts 
- Pricing, discounts, and promotions 
- Cart, checkout, and guest vs. logged-in flows 
- Orders, refunds, and return management 
- Subscriptions (if present) 
- Tenant/merchant configuration and branding 
- Basic analytics and reporting

- Identify missing or underdeveloped features important for Nigeria/Africa-focused commerce (e.g., cash-on-delivery variants, mobile-money, local logistics integrations).

Output: 
- A feature backlog: each proposed feature with rationale, priority, and indicative effort.

#### 5.7 Cloudflare, CI/CD, and infra

- Review Cloudflare Workers/D1/Pages setup and how bindings are managed per environment (dev/staging/prod).
- Analyze CI/CD (GitHub Actions or other) for tests, migrations, deploy previews, and production deploys.

Output: 
- Recommendations to harden CI/CD: staged rollouts, preview deployments, safe migrations for D1, rollback strategies, and consistent environment variable management.

#### 5.8 PWA, mobile, and offline-first

- Audit service worker config, caching strategies, and offline flows for storefront and, where appropriate, merchant tools.
- Check web app manifest, icons, install prompts, and UX on low-end Android devices. 

Output: 
- Detailed plan to reach strong PWA/offline behavior aligned with “Mobile First, PWA First, Offline First”.

#### 5.9 Internationalization and localization

- Evaluate i18n support (languages, formatters) and how easily Nigeria/Africa-first locales and languages can be supported.
- Check currency handling (NGN and others), date/time, and regional content for correctness and flexibility. 

Output: 
- Recommendations for robust and extensible i18n and multi-currency support (including price display, rounding, and exchange-rate strategies).

### 6. Final deliverable format

Create a **single, well-structured markdown report** named `COMMERCE_SUITE_REVIEW_AND_ENHANCEMENTS.md` in the repo root with:

1. Executive summary (5–10 bullet points) highlighting highest-impact findings and recommendations. 
2. Repo overview (architecture, key modules, how it fits into WebWaka via Super Admin V2). 
3. Test status and coverage roadmap. 
4. A dedicated section for each dimension (UI/UX, security/payments, performance, reliability, DX, features, infra/CI-CD, PWA/offline, i18n), each with: 
- Current state summary. 
- Identified gaps. 
- Recommended enhancements with priority and estimated effort. 
5. A **prioritized implementation roadmap** broken into phases (e.g., Phase 1: Security + Payments + Tests, Phase 2: PWA + Performance, Phase 3: Features & i18n), designed so future agents can implement changes incrementally with clean GitHub PRs.

Do **not** start implementing code changes until explicitly instructed in a follow-up message. Respond here when the report is complete with:

> `COMMERCE SUITE REVIEW COMPLETE — report generated, ready for implementation phase.`